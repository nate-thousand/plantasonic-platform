import type { ApplicationConfig } from '@plantasonic/platform-types';

/** Demo application configuration */
export const demoConfig: ApplicationConfig = {
  id: 'platform-demo',
  name: 'Plantasonic Platform Demo',
  description:
    'Minimal scaffold proving the platform shape — no real engines integrated yet.',
};
