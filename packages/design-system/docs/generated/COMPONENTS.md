# Component Reference (Generated)

Bootstrap showcase categories shipped with the design system.

Regenerate: `npm run generate:component-docs`

## Bootstrap categories

- `buttons` — see showcase → Bootstrap → buttons
- `cards` — see showcase → Bootstrap → cards
- `dialogs` — see showcase → Bootstrap → dialogs
- `disclosure` — see showcase → Bootstrap → disclosure
- `feedback` — see showcase → Bootstrap → feedback
- `floating` — see showcase → Bootstrap → floating
- `forms` — see showcase → Bootstrap → forms
- `lists` — see showcase → Bootstrap → lists
- `navigation` — see showcase → Bootstrap → navigation
- `selection` — see showcase → Bootstrap → selection
- `tables` — see showcase → Bootstrap → tables
- `utilities` — see showcase → Bootstrap → utilities

## Plantasonic components

Milestone 3 instrument components are documented in the showcase when available.
