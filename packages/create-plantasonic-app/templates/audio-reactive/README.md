# {{APP_NAME}}

Platform consumer generated by `pnpm plantasonic create {{PROTOTYPE_TYPE}} {{APP_SLUG}}`.

Use `--concept <id>` to apply an application blueprint or concept template.

**Blueprints** (full startup experience): `signal-9`  
**Concepts** (identity only): `plantasonic`, `flowers`

Example:

```bash
pnpm plantasonic create audio-reactive my-app --concept signal-9
```

## Run

```bash
pnpm install
pnpm dev
```

Opens at http://localhost:{{PORT}}.
