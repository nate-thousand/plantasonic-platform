# {{APP_NAME}}

Platform consumer generated by `pnpm plantasonic create {{PROTOTYPE_TYPE}} {{APP_SLUG}}`.

Use `--concept <id>` to apply an application blueprint or concept template (`signal-9`, `plantasonic`, `flowers`).

## Run

```bash
pnpm install
pnpm dev
```

Opens at http://localhost:{{PORT}}.
