# {{APP_NAME}}

Thin platform consumer generated by `create-plantasonic-app`.

## Run

```bash
pnpm install
pnpm dev
```

Opens at http://localhost:{{PORT}}.

## What this app owns

- App configuration and branding
- Preset bundle definitions
- Plugin manifests (optional)

## What this app does not own

- Design tokens, shell, or reusable UI (Design System)
- Sound/visual engine internals (platform adapters)
- Plugin runtime, persistence, MIDI routing (platform SDK)

See [docs/PLANTASONIC_APP_MIGRATION.md](../../docs/PLANTASONIC_APP_MIGRATION.md) in the platform repo.
